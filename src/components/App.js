import React, { Fragment } from 'react';
import { Header, Content, Footer } from './layouts';

const App = (props) => {
  return (
    <Fragment>
      <Header />
      <Content />
      <Footer />
    </Fragment>
  );
}

export default App;
